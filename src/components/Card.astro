---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`bg-white rounded-xl shadow-md p-6 border border-slate-100 ${className}`}>
  <slot />
</div>
